apiVersion: v1
kind: Service
metadata:
  name: redis-service
  labels:
    name: redis-service
    app: guestplaybook
spec:
  selector:
    name: redis-pod
    app: guestplaybook
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379